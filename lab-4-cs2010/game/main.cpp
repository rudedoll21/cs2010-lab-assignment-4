#include "game.h"

int main() {
    // Initialize the game
    initializeGame();

    // Run the game loop
    runGame();

    // End the game and clean up
    endGame();

    return 0;
}
